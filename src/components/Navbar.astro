<nav>
    <div class="nav-container">
        <div class="brd-logo">
            <img src="https://www.breadriuss.com/logo_recortado.png" alt="Breadriuss Logo">
        </div>

        <ul class="nav-links">
            <li><a href="/">Inicio</a></li>
            <li><a href="/acerca-de">Acerca de</a></li>
        </ul>
    </div>
</nav>

<style>
    .brd-logo {
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .brd-logo img {
        width: 40px;
        height: 40px;
        object-fit: contain;
        transform: translateY(-1.5px);
    }

    .nav-links {
        display: flex;
        align-items: center;
        justify-content: space-between;
        color: aliceblue;
        gap: 2rem;
        margin: 0;
        padding: 0;
        transition: all 0.3s ease;
    }

    .nav-links li {
        list-style: none;
    }

    .nav-links a {
        color: #e4e4e4;
        text-decoration: none;
        font-size: 0.9rem;
        transition: color 0.3s ease;
    }

    .nav-links a:hover {
        color: #ffffff;
    }

    nav {
        display: flex;
        align-items: center;
        justify-content: center;
        width: 100%;
        position: sticky;
        top: 0;
        z-index: 3;
    }

    nav .nav-container {
        display: flex;
        align-items: center;
        justify-content: space-between;
        width: 68%;
        padding: 0.6rem 1.4rem;
        border: 1px solid rgba(37, 37, 37, 0.2);
        margin-top: 1rem;
        border-radius: 30px;
        backdrop-filter: blur(10px);
        background: linear-gradient(to right, rgba(15, 15, 15, 0.486), rgba(19, 19, 19, 0.486));
        position: relative;
    }

    @media screen and (max-width: 768px) {
        nav .nav-container {
            width: 80%;
            padding: .6rem 1.2rem;
        }
    }
</style>